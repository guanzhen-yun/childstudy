<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/white">

  <com.tantan.child.view.numview.NumberParentView
    android:id="@+id/num_parent"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="20dp"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent" />

  <TextView
    android:id="@+id/tv_num"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="0"
    android:textColor="@color/black"
    android:textSize="90sp"
    android:textStyle="bold"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent" />

  <TextView
    android:id="@+id/tv_add"
    style="@style/style_num_tv_button"
    android:background="@drawable/bg_ripper"
    android:text="增加"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintRight_toRightOf="parent" />

  <TextView
    android:id="@+id/tv_minus"
    style="@style/style_num_tv_button"
    android:background="@drawable/bg_ripper"
    android:text="减少"
    android:textColor="#999999"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintLeft_toLeftOf="parent" />

  <TextView
    android:id="@+id/tv_auto_add"
    style="@style/style_num_tv_button"
    android:layout_marginBottom="10dp"
    android:background="@drawable/bg_ripper"
    android:text="自动增加"
    app:layout_constraintBottom_toTopOf="@id/tv_add"
    app:layout_constraintRight_toRightOf="parent" />

  <TextView
    android:id="@+id/tv_auto_minus"
    style="@style/style_num_tv_button"
    android:layout_marginBottom="10dp"
    android:background="@drawable/bg_ripper"
    android:text="自动减少"
    android:textColor="#999999"
    app:layout_constraintBottom_toTopOf="@id/tv_minus"
    app:layout_constraintLeft_toLeftOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>